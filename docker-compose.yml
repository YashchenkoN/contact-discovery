version: '3.3'

services:
  contact-discovery-query:
    image: contact-discovery-query:latest
    depends_on:
      - elasticsearch
      - mongo
    environment:
      - WAIT_HOSTS=elasticsearch:9200,mongo:27017

  contact-discovery-service:
    image: contact-discovery-service:latest
    depends_on:
      - elasticsearch
      - mongo
    environment:
      - WAIT_HOSTS=elasticsearch:9200,mongo:27017

  elasticsearch:
    image: elasticsearch:latest
    command: elasticsearch
    environment:
      - action.auto_create_index=true
    healthcheck:
      test: ["CMD-SHELL", "curl --silent --fail localhost:9200/_cluster/health || exit 1"]
      interval: 30s
      timeout: 30s
      retries: 3

  mongo_connector:
    build:
      ./mongo-connector
    depends_on:
      - mongo
      - elasticsearch
    environment:
      - WAIT_HOSTS=elasticsearch:9200,mongo:27017

  mongo:
    image: mongo:latest
    command: "mongod --replSet rs0"
    ports:
      - "27017:27017"